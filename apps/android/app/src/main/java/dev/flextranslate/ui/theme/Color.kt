package dev.flextranslate.ui.theme

import androidx.compose.ui.graphics.Color

// Aquacard graphite layered surfaces — depth via layering only, never shadows.
val Bg = Color(0xFF0E0F11)
val SurfaceColor = Color(0xFF16181C)
val SurfaceElevated = Color(0xFF1E2126)
val SurfaceHighest = Color(0xFF24272D)
val OutlineColor = Color(0xFF2A2E35)
val OutlineVariantColor = Color(0xFF232730)

// Text
val TextPrimary = Color(0xFFE6E8EB)
val TextSecondary = Color(0xFF9BA1A8)

// Single restrained accent — cool indigo
val Accent = Color(0xFF7C9CFF)
val OnAccent = Color(0xFF0B1020)
val AccentContainer = Color(0xFF1B2233)
val OnAccentContainer = Color(0xFFC9D5FF)

// Error (muted)
val ErrorColor = Color(0xFFFF6B6B)
val ErrorContainerColor = Color(0xFF2A1A1C)
val OnErrorContainerColor = Color(0xFFFFC2C2)

// Semantic palette — reserved for language / tier / confidence badges (WS-later).
val SemanticGreen = Color(0xFF22C55E)
val SemanticAmber = Color(0xFFF59E0B)
val SemanticRed = Color(0xFFEF4444)
val SemanticPurple = Color(0xFFA855F7)
val SemanticPink = Color(0xFFE879F9)
